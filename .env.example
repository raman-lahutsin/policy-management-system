# Database credentials (used by both PostgreSQL and Rails)
DB_USERNAME=example
DB_PASSWORD=example

# Rails master key (contents of backend/config/master.key)
# REQUIRED - copy the value from backend/config/master.key
RAILS_MASTER_KEY=

# Optional
RAILS_LOG_LEVEL=info
